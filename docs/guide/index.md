# 使用指南

::: warning 这部分还没写
使用指南（安装、添加作者、同步与下载、直播录制、标签与 AI 分析等）暂时是空的，
目录先占好位置。当前完整的功能说明请看仓库根目录的
[README](https://github.com/Everless321/dYm/blob/main/README_CN.md)。

已经写好的是[自定义脚本文档](/scripts/)。
:::

## 计划中的章节

- **快速上手** —— 安装、首次配置 cookie、添加第一个作者
- **同步与下载** —— 下载任务、下载上限、自动同步 cron
- **直播录制** —— 开播检测、录制参数、ffmpeg 依赖
- **AI 分析与标签** —— 分析配置、标签库、批量重标
- **设置项参考** —— 每个开关的实际作用

## 想补内容？

文档源文件在仓库的 `docs/` 目录下，是普通的 Markdown。
本地起站点：

```bash
npm run docs:dev
```

改完提 PR 即可。写作时的两条约定：

- 涉及具体行为（默认值、限制、报错文案）先去代码里核对一遍再写，别凭印象
- 面向使用者写，不要写实现细节；实现细节属于代码注释
